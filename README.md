# GameplayTagsEnhanced
Unreal Engine plugin that provides blueprint accessible GameplayTag utility nodes. Primary purpose is to simply expose the underlying C++ to BP.

At the moment this just exposes RequestGameplayTag to blueprint to allow converting a Name to a GameplayTag.
